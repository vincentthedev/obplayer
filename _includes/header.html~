<header class="site-header">

    <div class="wrapper">

        <h1><a class="site-title" href="{{ site.baseurl }}/">{{ site.title }}</a></h1>
        <h2 class="site-tagline">{{ site.tagline }}</h2>


        <nav class="site-nav">
            <a href="{{ site.url }}/">Home</a>
            {% for page in site.pages %}
                {% unless page.exclude %}
                {% if page.title %}
                <a class="page-link" href="{{ page.url | prepend: site.baseurl }}">{{ page.title }}</a>
                {% endif %}
                {% endunless %}
            {% endfor %}
            <a href="https://github.com/openbroadcaster/Player">GitHub Repo</a>
            <a href="https://github.com/openbroadcaster/Player/archive/master.zip">Download</a>
        </nav>

    </div> <!-- .wrapper -->

</header>
